<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/spacing_medium"
    android:background="@drawable/background_waveform"
    android:layout_margin="@dimen/spacing_medium">

    <com.masoudss.lib.WaveformSeekBar
        android:id="@+id/waveformSeekBar"
        android:layout_width="match_parent"
        android:layout_height="20dp"
        android:layout_margin="16dp"
        app:wave_background_color="@color/teal_deep"
        app:wave_corner_radius="4dp"
        app:wave_gap="2dp"
        app:wave_gravity="center"
        app:wave_progress_color="@color/gold"
        app:wave_width="3dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_play_message"
        style="@style/Widget.Material3.Button.IconButton"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:layout_marginTop="@dimen/spacing_xsmall"
        android:insetTop="0dp"
        android:insetBottom="0dp"
        android:padding="0dp"
        app:backgroundTint="@android:color/transparent"
        app:icon="@drawable/ic_play"
        app:iconGravity="textStart"
        app:iconPadding="0dp"
        app:iconTint="@color/teal_deep" />

</LinearLayout>